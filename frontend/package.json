{
  "name": "laravel-api-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Front sencillo en React para consumir la API de Laravel.",
  "scripts": {
    "web": "vite --host --port 8000 --strictPort",
    "dev": "vite --host --port 8000 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host --port 8000 --strictPort"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.0"
  }
}
